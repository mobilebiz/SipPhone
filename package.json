{
  "name": "SipPhone",
  "version": "1.0.6",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env",
    "deploy": "npm version patch --force && twilio serverless:deploy -p gij.com --runtime node12 --force"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "@kintone/rest-api-client": "^1.7.2"
  }
}
