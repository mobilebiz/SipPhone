{
  "name": "SipPhone",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "@kintone/rest-api-client": "^1.7.2"
  }
}
